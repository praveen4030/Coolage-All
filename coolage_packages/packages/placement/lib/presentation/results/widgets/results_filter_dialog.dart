import 'package:flutter/material.dart';

class ResultsFilterDialog extends StatelessWidget {
  const ResultsFilterDialog({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
